# Superseded outputs — do not cite as revised results

These files are copied from repository commit f4a476d to document the original
analysis. They used interpolated inequality/NPL outcomes, a capped persistence
adjustment, incomplete quantile inference, and unsupported structural scenario
mapping and burden normalizations. They are not inputs to the revised paper.
The main `output/` directory contains corrected estimates and validation.
